
task downloadZipFile(type: Download) {
    src 'https://ia801507.us.archive.org/24/items/barcodes_202006/barcodes.zip'
    dest new File(buildDir, 'zips/')
    overwrite false
}


task downloadAndUnzipFile(dependsOn: downloadZipFile, type: Copy) {
    from zipTree(downloadZipFile.dest)
    into project.ext.ASSET_DIR
}


task extractDB(type: Copy) {
    dependsOn downloadAndUnzipFile
}

tasks.whenTaskAdded { task ->
    if (task.name == 'assembleDebug') {
        task.dependsOn 'extractDB'
    }
    if (task.name == 'assembleRelease') {
        task.dependsOn 'extractDB'
    }
}